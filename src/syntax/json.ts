import { cyan } from './../theme/colors';

const json = [
  {
    name: 'JSON constants',
    scope: [
      'constant.language.json'
    ],
    settings: {
      foreground: cyan.cyan_500
    }
  }
];

export { json };